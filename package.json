{
  "name": "mvp-template",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "docker": "docker compose -f apps/backend/docker/docker-compose.dev.yml up -d",
    "docker:stop": "docker compose -f apps/backend/docker/docker-compose.dev.yml down",
    "dev": "turbo dev --filter=mobile --filter=backend",
    "dev:mobile": "turbo dev --filter=mobile",
    "dev:backend": "turbo dev --filter=backend",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules",
    "generate-env": "node scripts/generate-env.mjs",
    "setup": "npm install && node scripts/generate-env.mjs",
    "gitpush": "git push origin HEAD"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.3",
    "eslint": "^9.39.3",
    "eslint-plugin-react-hooks": "^7.0.1",
    "turbo": "^2.4.0",
    "typescript": "~5.6.0",
    "typescript-eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "expo-dev-client": "~6.0.20"
  }
}
